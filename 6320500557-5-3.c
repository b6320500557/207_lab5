#include <stdio.h>
int main()
{
    int n,i;
    scanf("%d",&n);
   /* for(i=1;i<=4000;i++)
    {
        if(i%2==0 || i%3==0 || i%5==0)
        {
            if(i==1)
                j++;
            else
                j++;
        }
        if(i == n)
            break;
    }
    printf ("%d",i);*/
    return 0;
}
